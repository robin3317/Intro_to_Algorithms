/*
  Informally, an algorithm is any well-defined computional procedure that takes
  some value, or set of values, as input and produces some value, or set of values, 
  as output. An algorithm is thus a sequence of computional steps that transform 
  the input into the output.
*/
